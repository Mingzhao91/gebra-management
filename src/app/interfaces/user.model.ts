// same as user.model.ts
export interface User {
  email: string;
  userId: string;
}

export interface DocUser {
  uid: string;
  email: string;
  firstName: string;
  lastName: string;
  englishName: string;
}
